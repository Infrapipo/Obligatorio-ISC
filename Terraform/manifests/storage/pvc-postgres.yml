apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claim-nfs-postgres
spec:
  accessModes:
    - ReadWriteMany
  volumeName: postgres-pv
  storageClassName: manual-nfs
  resources:
    requests:
      storage: 20Gi