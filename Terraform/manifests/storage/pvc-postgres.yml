apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claimefs-postgres
spec:
  accessModes:
    - ReadWriteMany
  volumeName: efs-pv-postgres
  storageClassName: ""
  resources:
    requests:
      storage: 20Gi